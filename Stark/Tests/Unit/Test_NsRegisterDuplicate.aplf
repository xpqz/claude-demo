 r←Test_NsRegisterDuplicate dummy;router;routes
 router←⎕NEW #.Stark
 router.Handlers←#.Mocks.MockHandlers
 routes←(
     (method: 'GET' ⋄ path: '/items' ⋄ handler: 'ListItems')
     (method: 'GET' ⋄ path: '/items' ⋄ handler: 'ListItems2')
 )
 :Trap 11
     router.Register routes
     r←0   ⍝ should not reach here
 :Else
     r←1   ⍝ duplicate detected
 :EndTrap
