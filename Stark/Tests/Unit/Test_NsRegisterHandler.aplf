 r←Test_NsRegisterHandler dummy;router;routes
 router←⎕NEW #.Stark
 router.Handlers←#.Mocks.MockHandlers
 routes←(
     (method: 'GET' ⋄ path: '/items/{id}' ⋄ handler: 'GetItem')
 ⋄)
 router.Register routes
 r←'GetItem'≡3⊃router.Routes[1;]
