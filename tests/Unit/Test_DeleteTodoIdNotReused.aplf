 r←Test_DeleteTodoIdNotReused dummy;app;req
 app←⎕NEW #.TodoApp(⊃1 ⎕NPARTS'./static/')
 ⍝ Create todo 1
 req←⎕NEW #.Mocks.MockRequest('POST' '/todos')
 req.Payload←⎕NS '' ⋄ req.Payload.title←,⊂'First'
 {}app.CreateTodo req
 ⍝ Delete todo 1
 req←⎕NEW #.Mocks.MockRequest('DELETE' '/todos/1')
 req.PathParams←⎕NS '' ⋄ req.PathParams.id←'1'
 {}app.DeleteTodo req
 ⍝ Create another
 req←⎕NEW #.Mocks.MockRequest('POST' '/todos')
 req.Payload←⎕NS '' ⋄ req.Payload.title←,⊂'Second'
 {}app.CreateTodo req
 r←1≠(⊃app.todos).id
