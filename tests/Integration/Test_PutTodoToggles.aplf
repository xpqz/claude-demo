 r←Test_PutTodoToggles dummy;cmd;resp;id
 ⍝ POST a todo, extract id from response, PUT to toggle
 cmd←⎕NEW #.HttpCommand
 cmd.(Command URL ContentType)←'POST'(BaseURL,'/todos')'application/x-www-form-urlencoded'
 cmd.Params←⎕NS '' ⋄ cmd.Params.title←'ToggleTest'
 resp←cmd.Run
 ⍝ Extract id from <li id="todo-N"
 id←(resp.Data⊆⍨resp.Data∊⎕D)~⊂''
 id←⊃id
 resp←#.HttpCommand.Do 'PUT'(BaseURL,'/todos/',id)
 r←∨/'class="completed"'⍷resp.Data
